import BarraLateral from "../../container/Barra"
import Formulario from "../../container/MeusContatos"

const Cadastro = () => (
  <>
    < BarraLateral mostrarFiltros={false} />
    < Formulario />
  </>
)

export default Cadastro
